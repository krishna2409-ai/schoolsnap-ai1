# V1 Development Dockerfile — Vite dev server with hot reload
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --silent
COPY . .
EXPOSE 3001
CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "3001"]
